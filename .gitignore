# Node.js and npm dependencies (for both client and server)
node_modules

# Production build output (for both client and server)
dist
build

# Environment variables (important to keep secret!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs and temporary files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
package-lock.json # While generally committed, can be ignored in some specific scenarios
yarn.lock       # Similar to package-lock.json

# IDE specific files
.idea/
.vscode/

# OS generated files
.DS_Store
Thumbs.db

# Optional: Ignore bundled dependencies if you build them separately
# client/dist
# server/build